{
  "_comment": "Copy to config.json on the Pico (git-ignored). Real WiFi + device secret NEVER go in git. The device_secret must match this device's entry in the socket-server DEVICE_SECRETS map.",
  "wifi_ssid": "REPLACE_ME",
  "wifi_password": "REPLACE_ME",
  "ws_url": "wss://socket.example.org",
  "device_id": "door-front",
  "device_secret": "REPLACE_ME",
  "door_id": "door-front",
  "timezone": "America/Chicago",
  "relay_pin": 15,
  "relay_active_high": true,
  "unlock_ms": 3000,
  "uart_id": 0,
  "uart_tx": 0,
  "uart_rx": 1,
  "uart_baud": 115200,
  "heartbeat_s": 20,
  "scan_timeout_s": 6,
  "_ota": "OTA (design #144). ota_base = socket-server HTTPS base; verify_key = DOOR_FW_VERIFY_KEY (spki DER base64, PUBLIC, from the vault). device_secret above is reused as the OTA fetch bearer.",
  "ota_base": "https://socket.example.org",
  "verify_key": "REPLACE_ME",
  "ota_poll_min": 60,
  "ota_max_tries": 3,
  "ota_max_bytes": 524288,
  "wdt_timeout_ms": 8000
}
