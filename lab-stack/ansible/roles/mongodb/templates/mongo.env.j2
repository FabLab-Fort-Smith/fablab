# Managed by Ansible (roles/mongodb) — root credentials for the MongoDB container. 0600, root-only.
# Consumed by compose as the container's env_file; never commit (@rules/workflow-secrets.md).
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD={{ mongodb_root_pw }}
